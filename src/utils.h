#ifndef UTILS_H
#define UTILS_H

#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <bitset>

using namespace std;

string decimal_to_binary(int decimal, int num_bits);

string decimal_to_hex(int decimal);

#endif